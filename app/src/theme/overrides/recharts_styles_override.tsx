import { css } from 'styled-components'

export const RechartsStylesOverride = css`
  .recharts-tooltip-cursor {
    z-index: 100;
  }

  .recharts-area-dots {
    z-index: 100;
  }

  .recharts-dot {
    z-index: 100;
    position: absolute;
  }
`
